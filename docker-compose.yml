services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-setlist}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-setlist}
      POSTGRES_DB: ${POSTGRES_DB:-setlistdb}
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-setlist} -d ${POSTGRES_DB:-setlistdb}"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
